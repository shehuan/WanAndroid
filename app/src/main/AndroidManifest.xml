<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.shehuan.wanandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/FullScreenTheme" />
        <activity android:name=".ui.login.LoginActivity" />
        <!--<activity android:name=".ui.main.MainActivity" />-->
        <activity android:name=".ui.about.AboutActivity" />
        <activity
            android:name=".ui.query.QueryActivity"
            android:theme="@style/AppTheme1" />
        <activity android:name=".ui.tree.treeDetail.TreeDetailActivity" />
        <activity
            android:name=".ui.chapter.chapterDetail.ChapterDetailActivity"
            android:theme="@style/AppTheme1" />
        <activity android:name=".ui.article.ArticleActivity" />
        <activity android:name=".ui.collection.MyCollectionActivity" />
        <activity android:name=".ui.register.RegisterActivity" />
    </application>

</manifest>